import { AbstractRequestContext } from '@tsdi/common';

/** UDP request context placeholder. */
export class UdpRequestContext extends AbstractRequestContext {
    // Context for a UDP request can be extended in the future as needed
}
