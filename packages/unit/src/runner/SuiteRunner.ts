import { IRunner } from '@ts-ioc/bootstrap';

/**
 * Suite runner.
 *
 * @export
 * @class SuiteRunner
 * @implements {IRunner<any>}
 */
export class SuiteRunner implements IRunner<any> {

    async run(data?: any): Promise<any> {

    }

}
