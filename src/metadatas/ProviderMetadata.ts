import { Metadate } from './Metadate';
import { Provider } from '../Provider';

/**
 * Provider metadata
 *
 * @export
 * @interface PropMetadata
 */
export interface ProviderMetadata extends Metadate, Provider {

}
