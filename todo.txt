1. @packages/microservices/ 基于client的provideClient 和 service的prvoideService，提供的feature
  特征及特征抽象拦截器，统一终端和服务提供入口（withSession(), withJson(), withBodyParser(), withContent(),
  withCors(), withHealth() 等等等），让各协议最终实现拦截器默认的拦截器及MessageReader; 
2. 继续实现CoAP， Http协议服务，并完成功通过能测试代码；
3. 继续完成其他协议的功能测试，并让验证测试通过；
4. HeaderAdapter, StatusAdapter移除了, 让具体协议直接通过MessageReader 读取
5. @packages/services/ 下的所有包已经过时，后面会删除， 不能直接引用这里面的代码，只参考




参考 @../hermes-agent/ 和 @../zeroclaw/ 这两个智能体， 继续完善 @packages/agents/agent-tools/  的工具，并参考添加这个两个智能体已有的工具



这次改动主要是：
  - 新增文件变更工具：write_file、edit_file
  - 新增 memory 工具：memory.put、memory.search
  - memory.list 增加 limit
  - 给 read_file / glob_search / content_search 补了 symlink 防护和 workspace pattern 边界校验
  - 修正 LocalToolRegistry 的重复工具名去重，让定义、inspect、invoke 语义一致
  - 将 write_file / edit_file 从默认 filesystem 分组拆到新的 filesystem_write，默认不启用

  关键位置：
  - packages/agents/agent-tools/files/write-file.tool.ts
  - packages/agents/agent-tools/files/edit-file.tool.ts
  - packages/agents/agent-tools/memory/memory-put.tool.ts
  - packages/agents/agent-tools/memory/memory-search.tool.ts
  - packages/agents/agent-tools/src/provider.ts
  - packages/agents/agent/src/tools/LocalToolRegistry.ts
  - packages/agents/agent-tools/test/tools.spec.ts

  验证结果：
  - cd /home/zhouyou/workspace/core/packages/agents/agent-tools && npm test
  - 结果：51 passing

  还剩两个我没有继续深入处理的安全设计问题，建议作为下一批：
  - write_file / edit_file 仍有更深层的 TOCTOU symlink race 风险，若要彻底收口，需要改成更严格的 fd/open 策略
  - memory.put/search/list/delete 对 global scope 目前仍偏宽松，若后续要支持多会话/多租户，建议加权限或 ACL

  如果你同意，我下一步继续做第二批：ask_user + process.start/poll/kill。
  